x-main-service: &main-service
  image: sf-search-main:latest
  build:
    context: .
    dockerfile: services/main/Dockerfile
  env_file:
    - path: services/main/.env
      required: false
  environment:
    DB_USER: ${POSTGRES_USER:-sf_search}
    DB_PASSWORD: ${POSTGRES_PASSWORD:-sf_search_dev_password}
    DB_HOST: postgres
    DB_PORT: 5432
    DB_NAME: ${POSTGRES_DB:-sf_search}
    DB_DRIVER: postgresql+psycopg
    NATS_URL: nats://nats:4222
    OBSERVABILITY_ENABLED: ${OBSERVABILITY_ENABLED:-false}
    OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://localhost:4317}
    OTEL_EXPORTER_OTLP_INSECURE: ${OTEL_EXPORTER_OTLP_INSECURE:-true}
    OTEL_SERVICE_VERSION: ${OTEL_SERVICE_VERSION:-local}
    DEPLOYMENT_ENVIRONMENT: ${DEPLOYMENT_ENVIRONMENT:-local}
    EMBEDDING_VECTOR_STORE_KIND: ${EMBEDDING_VECTOR_STORE_KIND:-qdrant}
    QDRANT_URL: http://qdrant:6333
    QDRANT_COLLECTION: ${QDRANT_COLLECTION:-panorama_view_embeddings_siglip2}
    QDRANT_VECTOR_ON_DISK: ${QDRANT_VECTOR_ON_DISK:-true}
    QDRANT_HNSW_ON_DISK: ${QDRANT_HNSW_ON_DISK:-false}
    QDRANT_ON_DISK_PAYLOAD: ${QDRANT_ON_DISK_PAYLOAD:-true}
    QDRANT_UPSERT_WAIT: ${QDRANT_UPSERT_WAIT:-true}
    QDRANT_TIMEOUT_SECONDS: ${QDRANT_TIMEOUT_SECONDS:-30}
    HF_HOME: /app/services/main/.local/huggingface
  volumes:
    - ./services/main/.local:/app/services/main/.local
    - ./services/main/target.geojson:/app/services/main/target.geojson:ro
    - ./docs:/app/docs:ro
  depends_on:
    postgres:
      condition: service_healthy
    nats:
      condition: service_healthy

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-sf_search}
      POSTGRES_USER: ${POSTGRES_USER:-sf_search}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-sf_search_dev_password}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - ./.local/postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\""]
      interval: 5s
      timeout: 5s
      retries: 10

  nats:
    image: nats:2-alpine
    command: ["-js", "-m", "8222", "--store_dir", "/data/jetstream"]
    ports:
      - "${NATS_PORT:-4222}:4222"
      - "${NATS_MONITOR_PORT:-8222}:8222"
    volumes:
      - nats-data:/data
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8222/healthz"]
      interval: 5s
      timeout: 5s
      retries: 10

  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "${QDRANT_HTTP_PORT:-6333}:6333"
      - "${QDRANT_GRPC_PORT:-6334}:6334"
    volumes:
      - ./.local/qdrant-storage:/qdrant/storage
    restart: unless-stopped

  discovery:
    <<: *main-service
    command: ["uv", "run", "python", "-m", "main_service.discovery"]
    profiles: ["pipeline"]
    restart: unless-stopped

  downloader:
    <<: *main-service
    command: ["uv", "run", "python", "-m", "main_service.downloader"]
    profiles: ["pipeline"]
    restart: unless-stopped

  processing:
    <<: *main-service
    command: ["uv", "run", "python", "-m", "main_service.processing"]
    profiles: ["pipeline"]
    restart: unless-stopped

  embedding:
    <<: *main-service
    command: ["uv", "run", "python", "-m", "main_service.embedding"]
    depends_on:
      postgres:
        condition: service_healthy
      nats:
        condition: service_healthy
      qdrant:
        condition: service_started
    profiles: ["pipeline"]
    restart: unless-stopped

  monitoring:
    <<: *main-service
    command: ["uv", "run", "python", "-m", "main_service.monitoring"]
    profiles: ["pipeline"]
    restart: unless-stopped

  query-ui:
    <<: *main-service
    command: ["uv", "run", "python", "-m", "main_service.embedding.query_ui", "--host", "0.0.0.0"]
    depends_on:
      postgres:
        condition: service_healthy
      nats:
        condition: service_healthy
      qdrant:
        condition: service_started
    ports:
      - "${QUERY_UI_PORT:-8787}:8787"
    profiles: ["query"]
    restart: unless-stopped

volumes:
  nats-data:
